diff --git a/cartridges/cache.py b/cartridges/cache.py
index 1e2dbfb..ae2a394 100644
--- a/cartridges/cache.py
+++ b/cartridges/cache.py
@@ -239,7 +239,7 @@ class TrainableCache(nn.Module):
         # Here, num_tokens is inferred from trainable keys, but note that the total tokens may be different if fixed tokens exist.
         # The number of fixed tokens can be inferred from frozen_keys if available.
         num_frozen_tokens = (
-            checkpoint["frozen_keys"][0].size(1) if checkpoint["frozen_keys"] else 0
+            checkpoint["frozen_keys"][0].size(2) if checkpoint["frozen_keys"] else 0
         )
 
         return cls(
diff --git a/cartridges/data/mtob/evals.py b/cartridges/data/mtob/evals.py
index 5cd2590..9e2dc75 100644
--- a/cartridges/data/mtob/evals.py
+++ b/cartridges/data/mtob/evals.py
@@ -31,7 +31,7 @@ Finally, provide the most likely translation enclosed within <answer> tags. For
 Respond with only the thinking steps followed by the final tagged answer."""
 
 
-chrf_metric = evaluate.load("chrf")
+chrf_metric = evaluate.load("chrf", revision="a2e19c654775e10357ed6c0234b3020390895e8f")
 
 
 
diff --git a/cartridges/data/mtob/resources.py b/cartridges/data/mtob/resources.py
index 0bb06d8..22da348 100644
--- a/cartridges/data/mtob/resources.py
+++ b/cartridges/data/mtob/resources.py
@@ -13,6 +13,7 @@ class MTOBResource(Resource):
         setup: Literal["latex_and_sentences", "medium_and_sentences"]
         seed_prompts: List[SEED_TYPES] = ["generic"]
         tokenizer: str = "Qwen/Qwen3-4b"
+        tokenizer_revision: str | None = None
         min_chunk_size: int = 512
         max_chunk_size: int = 4096
         
@@ -25,7 +26,7 @@ class MTOBResource(Resource):
             include_wordlist=False,
             include_sentences=True,
         )
-        self.tokenizer = AutoTokenizer.from_pretrained(self.config.tokenizer)
+        self.tokenizer = AutoTokenizer.from_pretrained(self.config.tokenizer, revision=self.config.tokenizer_revision)
 
         self.tokens = self.tokenizer.encode(self.content)
 
diff --git a/cartridges/initialization/tokenization_utils.py b/cartridges/initialization/tokenization_utils.py
index d3fdcbf..890f6bc 100644
--- a/cartridges/initialization/tokenization_utils.py
+++ b/cartridges/initialization/tokenization_utils.py
@@ -140,4 +140,5 @@ MODELS_WITH_THINKING = {
     "Qwen/Qwen3-8b",
     "Qwen/Qwen3-14b",
     "Qwen/Qwen3-32b",
-}
\ No newline at end of file
+}
+MODELS_WITH_THINKING |= {name[:-1] + "B" for name in MODELS_WITH_THINKING}
diff --git a/cartridges/train.py b/cartridges/train.py
index 648334e..f673e12 100644
--- a/cartridges/train.py
+++ b/cartridges/train.py
@@ -148,7 +148,7 @@ def train(config: TrainConfig):
     logger.info(f"Num devices: {num_devices}")
 
     logger.info(f"Train outputs will be saved to {config.run_dir}")
-    tokenizer = AutoTokenizer.from_pretrained(config.model.pretrained_model_name_or_path)    
+    tokenizer = AutoTokenizer.from_pretrained(config.model.pretrained_model_name_or_path, revision=getattr(config.model, "load_kwargs", {}).get("revision"))
 
     t0 = time.time()
     dataset = config.dataset.instantiate(tokenizer=tokenizer, seed=config.seed)
@@ -326,6 +326,8 @@ def train(config: TrainConfig):
     else:
         lr_scheduler = None
     for epoch_idx in range(1, config.epochs + 1):
+        if config.max_optimizer_steps >= 0 and optimizer_step >= config.max_optimizer_steps:
+            break
 
         if is_ddp and train_sampler is not None:
             train_sampler.set_epoch(epoch_idx)
@@ -339,6 +341,8 @@ def train(config: TrainConfig):
         )
         logger.info(f"Dataloader length: {len(dataloader)}")
         for batch in train_pbar:
+            if config.max_optimizer_steps >= 0 and optimizer_step >= config.max_optimizer_steps:
+                break
             batch: DatasetBatch
             do_step = (iter_idx + 1) % accumulate_grad_steps == 0
 
